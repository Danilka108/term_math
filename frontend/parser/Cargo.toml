[package]
name = "parser"
version = "0.1.0"
edition = "2021"

[lib]
name = "parser"
path = "src/lib.rs"

[dependencies]
error = { path = "../error" }
lexer = { path = "../lexer" }
validator = { path = "../validator" }
ast = { path = "../ast" }
